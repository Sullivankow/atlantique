# Activer le moteur de réécriture

RewriteEngine On

# Autoriser ton IP réelle (remplace par la tienne)

# RewriteCond %{REMOTE_ADDR} ^123\.456\.78\.90$

# Si ce n'est pas ton IP, rediriger vers maintenance.html

RewriteEngine On
RewriteCond %{REQUEST_URI} !/maintenance.html$
RewriteRule ^(.*)$ /maintenance.html [R=302,L]

# Sécurité : empêcher l'accès direct au fichier .htaccess

<Files .htaccess>
Order allow,deny
Deny from all
</Files>
